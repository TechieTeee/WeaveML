import { BundleQueryResponse, ReaderInterface } from "../reader.interface";
export interface Paginator {
    exmFunctionId: string;
    after?: string;
}
export declare class ArweaveReader implements ReaderInterface<Paginator, String> {
    fetchBundles(paginator: Paginator): Promise<BundleQueryResponse<String>[]>;
    private buildGqlQuery;
}
