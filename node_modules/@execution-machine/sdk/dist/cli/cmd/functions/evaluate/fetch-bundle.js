"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.cacheBundle = void 0;
const find_cache_dir_1 = __importDefault(require("find-cache-dir"));
const fs_1 = require("fs");
const path_1 = __importDefault(require("path"));
let __cacheDir = undefined;
const cacheBundle = async (bundleId, cache) => {
    if (cache) {
        __cacheDir = (0, find_cache_dir_1.default)({
            name: 'exm',
            create: true
        });
    }
    const fetchBundle = await fetch(`https://arweave.net/${bundleId}`);
    if (fetchBundle.ok) {
        const bundleData = await fetchBundle.json();
        if (cache) {
            if (__cacheDir) {
                (0, fs_1.writeFile)(path_1.default.join(__cacheDir, `${bundleId}.json`), JSON.stringify(bundleData), () => {
                });
            }
            else {
                console.log(`Cache Dir not found. Skipping ⏭️\n`);
            }
        }
        return bundleData;
    }
    throw new Error(`Bundle ${bundleId} could not be fetched`);
};
exports.cacheBundle = cacheBundle;
//# sourceMappingURL=fetch-bundle.js.map